// Re-export types
export type { LogContext, LogEntry, LoggerRef, LogLevel } from './LoggerModule';

// Re-export main API and component
export { Logger, default as LoggerModule } from './LoggerModule';
