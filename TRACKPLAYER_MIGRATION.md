# 🎵 Migration to react-native-track-player

## ✅ Что было сделано:

### 1. Установлено:

- ✅ `react-native-track-player` - для нативного фонового воспроизведения
- ✅ `expo-keep-awake` - оставлен для EventMonitor (expo-audio)

### 2. Созданные файлы:

- ✅ `src/services/trackPlayerService.ts` - утилиты для TrackPlayer
- ✅ `src/services/playbackService.ts` - обработчик фоновых событий

### 3. Изменённые файлы:

- ✅ `src/providers/PlaybackProvider.tsx` - **полностью переписан** на TrackPlayer
- ✅ `src/app/_layout.tsx` - регистрация TrackPlayer service
- ✅ `app.config.js` - настроен для TrackPlayer

### 4. Что НЕ изменилось (работает как раньше):

- ✅ `useCompactPlayerControls` - тот же API
- ✅ `Home screen (index.tsx)` - без изменений
- ✅ `Playlists screens` - без изменений
- ✅ `MiniPlayer.tsx` - без изменений
- ✅ `WebPlayerBar.tsx` - без изменений
- ✅ `EventMonitor.tsx` - остался на expo-audio

---

## 🚀 Следующие шаги:

### Шаг 1: Пересобрать приложение

**ВАЖНО:** Нужен новый нативный билд!

```bash
cd deezeroom

# Очистить и пересобрать нативный код
npx expo prebuild --clean

# Запустить на устройстве
npx expo run:android
# или
npx expo run:ios
```

### Шаг 2: Тестирование

#### 1. Home Search Playback:

- [ ] Найти треки через поиск
- [ ] Запустить воспроизведение
- [ ] Заблокировать экран
- [ ] Дождаться окончания трека
- [ ] **Ожидание:** Автоматическое переключение на следующий трек

#### 2. Lock Screen Controls:

- [ ] Во время воспроизведения заблокировать экран
- [ ] **Ожидание:** Появятся нативные медиа-контролы
- [ ] Попробовать play/pause, next, previous
- [ ] **Ожидание:** Все работает

#### 3. Notification Controls (Android):

- [ ] Во время воспроизведения свернуть приложение
- [ ] **Ожидание:** Уведомление с медиа-контролами
- [ ] Попробовать управлять из уведомления

#### 4. Playlists Playback:

- [ ] Открыть playlist
- [ ] Запустить воспроизведение
- [ ] Проверить фоновое переключение

#### 5. Events (expo-audio):

- [ ] Открыть event
- [ ] Запустить EventMonitor
- [ ] **Ожидание:** Работает как раньше (expo-audio)

---

## 🔍 Отладка

### Логи TrackPlayer:

```bash
# Android
adb logcat | grep TrackPlayer
adb logcat | grep PlaybackProvider

# iOS
# В Xcode console фильтр: TrackPlayer
```

### Проверка состояния:

```typescript
// В консоли разработчика
import TrackPlayer from 'react-native-track-player';

// Текущее состояние
await TrackPlayer.getPlaybackState();

// Текущий трек
await TrackPlayer.getActiveTrack();

// Очередь
await TrackPlayer.getQueue();
```

---

## ⚠️ Возможные проблемы:

### 1. "Cannot read property 'CAPABILITY_PLAY' of null"

**Причина:** Приложение запущено через Expo Go
**Решение:** Используйте `npx expo run:android` для development build

### 2. "TrackPlayer is not initialized"

**Решение:** Подождите несколько секунд после запуска приложения

### 3. Треки не переключаются в фоне

**Проверить:**

- [ ] Приложение собрано с `npx expo run:android/ios` (не Expo Go)
- [ ] `UIBackgroundModes: ['audio']` в app.config.js
- [ ] Battery optimization отключена на устройстве

### 4. EventMonitor не работает

**Проверить:**

- [ ] EventMonitor всё ещё использует `useAudioPlayer` из expo-audio
- [ ] Это нормально - events работают отдельно

---

## 📊 Архитектура:

```
Main Playback (Search/Playlists)
         ↓
  PlaybackProvider
         ↓
  react-native-track-player
         ↓
  Нативное фоновое воспроизведение
  + Lock screen controls
  + Notification controls


Events Playback (EventMonitor)
         ↓
  useAudioPlayer (expo-audio)
         ↓
  Динамическая очередь + Firebase
```

---

## 🎯 Преимущества:

- ✅ **Автоматическое переключение треков** в фоне
- ✅ **Lock screen controls** (iOS + Android)
- ✅ **Notification media controls** (Android)
- ✅ **Нативная производительность**
- ✅ **Не требует wake lock** (не держит экран)
- ✅ **Events остались гибкими** (expo-audio для динамической очереди)

---

## 🔧 Регистрация Service:

TrackPlayer service зарегистрирован в `src/app/_layout.tsx`:

```typescript
import TrackPlayer from 'react-native-track-player';
import { PlaybackService } from '@/services/playbackService';

// Регистрация при инициализации модуля
TrackPlayer.registerPlaybackService(() => PlaybackService);
```

Это правильный подход для Expo Router - не нужно менять `package.json` main!

---

## 🆘 Поддержка:

- Документация: https://rntp.dev/
- GitHub: https://github.com/doublesymmetry/react-native-track-player
